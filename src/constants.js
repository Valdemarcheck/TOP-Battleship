// board settings
export const TILE_SIZE_PX = 45;
export const BOARD_SIZE = 10;

// ship over tile detection
export const MAX_HORIZONTAL = parseInt(TILE_SIZE_PX / 2);
export const MAX_VERTICAL = parseInt(TILE_SIZE_PX / 2);
